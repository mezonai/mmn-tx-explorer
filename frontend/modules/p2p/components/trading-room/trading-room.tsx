'use client';

interface TradingRoomProps {
  orderId: string;
  currentUserId?: string;
}

// Temporary stub: full trading room UI and hooks were removed.
// This placeholder keeps the module compiling until the new P2P flow is reintroduced.
export const TradingRoom = (_props: TradingRoomProps) => {
  return null;
};
