import { ComingSoon } from '@/components/shared';

interface CreateCampaignProps {}

export const CreateCampaign = async ({}: CreateCampaignProps) => {
  return <ComingSoon title="Create Campaign" />;
};
